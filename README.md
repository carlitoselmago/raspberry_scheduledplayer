# Raspberry scheduled player

Create a service to run the script

Edit the schedule on the file video_schedule.txt

Install dependencies
```
sudo apt update 
sudo apt install -y fim imagemagick ffmpeg
```